# Contracts Package

Shared API and event contracts for the desktop frontend and Python engine.

- `schemas/`: JSON Schema source of truth.
- `python/contracts/`: Python Pydantic models mirroring schemas.
- `ts/`: TypeScript types mirroring schemas.

Current contract version: `1.0.0`
